{% extends 'base.html.twig' %}

{% block title %}OffrePublicitaire{% endblock %}

{% block body %}
    <h1>OffrePublicitaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdOffre</th>
                <td>{{ offre_publicitaire.idOffre }}</td>
            </tr>
            <tr>
                <th>NomOffre</th>
                <td>{{ offre_publicitaire.nomOffre }}</td>
            </tr>
            <tr>
                <th>ContenuOffre</th>
                <td>{{ offre_publicitaire.contenuOffre }}</td>
            </tr>
            <tr>
                <th>NbrMaxOffre</th>
                <td>{{ offre_publicitaire.nbrMaxOffre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_publicitaire_index') }}">back to list</a>

    <a href="{{ path('app_offre_publicitaire_edit', {'idOffre': offre_publicitaire.idOffre}) }}">edit</a>

    {{ include('offre_publicitaire/_delete_form.html.twig') }}
{% endblock %}
